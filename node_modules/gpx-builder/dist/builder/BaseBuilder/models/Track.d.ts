import { Track as TrackData, Extensions } from '../../../types';
import Link from './Link';
import Segment from './Segment';
export default class Track {
    private name;
    private cmt;
    private desc;
    private src;
    private link;
    private number;
    private type;
    private extensions;
    private trkseg;
    /**
     * @see http://www.topografix.com/gpx/1/1/#type_trkType
     */
    constructor(trkseg: Segment[] | null, { name, cmt, desc, src, link, number, type, extensions, }?: {
        name?: string;
        cmt?: string;
        desc?: string;
        src?: string;
        link?: Link;
        number?: number;
        type?: string;
        extensions?: Extensions;
    });
    setSegments(trkseg: Segment[] | null): this;
    toObject(): TrackData;
}
